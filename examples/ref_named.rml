val double : int[x] -> int[y | y = (x + x)] ;;
let double = (fun (x: int) -> x + x) ;;

val inc : int[x] -> int[y | (y - x) = 1] ;; 
let inc = (fun (x: int) -> x + 1) ;;

val add : int -> int[c | c > 0] -> int[y | y > x] ;; 
let add = (fun (x: int) -> 
    (fun (c: int[c | c > 0]) -> x + c)
) ;; 

